@using HealthSched.Models.Models.Concrete
@* @model Title *@

@* <div class="container my-5">
    
    <table class="table table-bordered table-striped text-center">
        <thead>
            <tr>
                <th>Doktor Adı Soyadı</th>
                <th>Polikliniği</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var doctor in Model)
            {
                <tr class="align-middle">
                    <td>@doctor.FullName</td>
                    <td>@doctor.Policlinic.PoliclinicName</td>
                    <td>
                </tr>
            }
        </tbody>
    </table>
</div>
 *@

@model HealthSched.Models.Models.Concrete.Title

<div class="container my-5">
    <h2>@Model.TitleName</h2>
    <table class="table table-bordered table-striped text-center">
        <thead>
            <tr>
                <th>Doktor Adı</th>
                <th>Poliklinik</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var doctor in Model.Doctors)
            {
                <tr class="align-middle">
                    <td>@doctor.FullName</td>
                    <td>@doctor.Policlinic.PoliclinicName</td>
                </tr>
            }
        </tbody>
    </table>
</div>
